generator photon {
  provider = "photonjs"
}

datasource db {
  provider = "sqlite"
  url = "***"
}

model Flat {
  id   String @default(cuid()) @id
  name String
}

model User {
  id   String @default(cuid()) @id
}