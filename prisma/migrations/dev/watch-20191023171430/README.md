# Migration `watch-20191023171430`

This migration has been generated by schickling at 10/23/2019, 5:14:30 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE UNIQUE INDEX "lift"."Flat.immoId" ON "Flat"("immoId")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration watch-20191023170931..watch-20191023171430
--- datamodel.dml
+++ datamodel.dml
@@ -3,19 +3,19 @@
 }
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url      = "file:dev.db"
 }
 model Flat {
-  id        String  @default(cuid()) @id
-  immoId    String
-  year      Int
-  size      Float
-  bathroom  Boolean
-  heating   Boolean
-  coldRent  Float
+  id       String  @default(cuid()) @id
+  immoId   String  @unique
+  year     Int
+  size     Float
+  bathroom Boolean
+  heating  Boolean
+  coldRent Float
   // warmRent  Float
   // maxRent   Float
   // latitude  Float
   // longitude Float
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20191023171430)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20191023171430'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
