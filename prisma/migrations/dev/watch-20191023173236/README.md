# Migration `watch-20191023173236`

This migration has been generated by schickling at 10/23/2019, 5:32:36 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_Flat" (
  "bathroom" BOOLEAN NOT NULL DEFAULT false  ,
  "coldRent" REAL NOT NULL DEFAULT 0  ,
  "heating" BOOLEAN NOT NULL DEFAULT false  ,
  "id" TEXT NOT NULL   ,
  "immoId" TEXT NOT NULL DEFAULT ''  ,
  "pictureUrl" TEXT NOT NULL DEFAULT ''  ,
  "size" REAL NOT NULL DEFAULT 0  ,
  "year" INTEGER NOT NULL DEFAULT 0  ,
  PRIMARY KEY ("id")
);

INSERT INTO "new_Flat" ("bathroom","coldRent","heating","id","immoId","size","year") SELECT "bathroom","coldRent","heating","id","immoId","size","year" from "Flat"

DROP TABLE "lift"."Flat";

ALTER TABLE "lift"."new_Flat" RENAME TO "Flat";

CREATE UNIQUE INDEX "lift"."Flat.immoId" ON "Flat"("immoId")

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration watch-20191023171430..watch-20191023173236
--- datamodel.dml
+++ datamodel.dml
@@ -3,19 +3,20 @@
 }
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url      = "file:dev.db"
 }
 model Flat {
-  id       String  @default(cuid()) @id
-  immoId   String  @unique
-  year     Int
-  size     Float
-  bathroom Boolean
-  heating  Boolean
-  coldRent Float
+  id         String  @default(cuid()) @id
+  immoId     String  @unique
+  year       Int
+  size       Float
+  bathroom   Boolean
+  heating    Boolean
+  coldRent   Float
+  pictureUrl String
   // warmRent  Float
   // maxRent   Float
   // latitude  Float
   // longitude Float
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20191023173236)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20191023173236'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
